#### 7) Create a README file to answer the following questions-

> **⚠️ Warning:** Do not use any AI tools to answer these questions. You must write the answers in **Bangla**.

#### 1) What is the difference between `null` and `undefined`?
> 1A: The difference between `null` and `undefined` is, `null` is a object type data and `undefined` is undefine type data.. we dont use undefined as value of varriable but we use null as a value of varriable

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?
> 2A: The use of map is to transform data by going through each item of array and take a new array after changing items and it is different from forEach() because forEach also go through every item but doesnt give back a new array

#### 3) What is the difference between `==` and `===`?
> 3A: The difference between `==` and `===` is `==` is used to check the value of data but `===` is used to check the data type if the data type match with each other then it check the values

#### 4) What is the significance of `async`/`await` in fetching API data?
> 4A: the significance of async/ await in fetching data is it provides clear and structured way to handle asyncrhonus API requests and it maked the code easier to read, manage and maintain

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).
> 5A: Scope in Javascript defines where a varriable can be accessed in a coded... In a global scope varriables which are declared out of any functions or block can be accessed from anywhere, in Function scope a varriable which is declared in a function can be accessed only in that function, and in a block scope varriable which are declared with let or const inside a object can be accessed with in that block
